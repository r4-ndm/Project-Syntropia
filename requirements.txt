# Core Dependencies for Project Syntropia
mido>=1.3.0
python-rtmidi>=1.5.0
psutil>=5.9.0
pydantic>=2.0.0

# LLM Local CPU inference (requires compilation during installation)
llama-cpp-python>=0.2.0

# Note on P2P Torrent dependencies:
# libtorrent is best installed via system package manager (e.g. `sudo apt install python3-libtorrent`)
# but can also be attempted via pip:
# libtorrent>=2.0.0
